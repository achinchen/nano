import { DatePickerMonthLoose, DatePickerMonthTight } from '.';

describe('DatePickerMonth entry', () => {
  it('has Loose', () => {
    expect(DatePickerMonthLoose).toBeDefined();
  });

  it('has Tight', () => {
    expect(DatePickerMonthTight).toBeDefined();
  });
});
